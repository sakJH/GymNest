version: '3.8'
services:
  app-user-management-service:
    build: .
    ports:
      - "3001:3001"
    depends_on:
      - db-user-management-service
    env_file:
      - .env
    volumes:
      - .:/usr/src/user-management-service
      - /usr/src/user-management-service/node_modules
    environment:
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
    command: npm start

  db-user-management-service:
    image: mysql:5.7
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_MYSQL_PASS}
      MYSQL_DATABASE: ${DB_NAME}
    volumes:
      - db_data:/var/lib/mysql
      - ./user-management-service/database/migrations:/docker-entrypoint-initdb.d/init_gymnest_user_db.sql

volumes:
  db_data: