version: '3.8'
services:
  app-management-service:
    build: .
    ports:
      - "3002:3002"
    depends_on:
      - db-management-service
    env_file:
      - .env
    volumes:
      - .:/usr/src/member-service
      - /usr/src/member-service/node_modules
    environment:
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
    command: npm start

  db-management-service:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_MYSQL_PASS}
      MYSQL_DATABASE: ${DB_NAME}
    volumes:
      - db_data:/var/lib/mysql
      - ./database/migrations:/docker-entrypoint-initdb.d/init_gymnest_membership_db.sql
    command: --default-authentication-plugin=mysql_native_password

volumes:
  db_data:
